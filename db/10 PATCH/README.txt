Here you find SQL patchfiles to upgrade a current db schema to a new version
Each patch is inside a folder named after the new version
Inside you find information which version is required to apply the patch
and all SQL files you have to execute